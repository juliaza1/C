Program is working fine.

Previous pointer in linked list: to make it easier to iterate the linked list I added a reverse pointer to the node struct.
It is possible to infinitely recurse using the h-command.

The main function is a little bit cluttered.
